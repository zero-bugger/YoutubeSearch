import { SearchResultClass } from './search-result-class';

describe('SearchResultClass', () => {
  it('should create an instance', () => {
    expect(new SearchResultClass()).toBeTruthy();
  });
});
